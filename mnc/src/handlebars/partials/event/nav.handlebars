<h3 class="listTitle">{{title}}</h3>
<div class="listBox">
    {{#each eventMenu}}
    <div class="listItem">
        <a href="{{link}}" class="listLink{{#eq ../page compare=title}} active{{/eq}}">{{title}}</a>
    </div>
    {{/each}}
</div>