<!DOCTYPE html>
<html lang="en">
{{> head}}

<body>

	<!-- header -->
	{{> header login=true}}
	<!-- header -->

	<!-- main -->
	<main role="main">
		{{> ads/skin}}
		{{> ads/billboard}}
		<section class="container">
			{{> event}}
			<div class="row display-flex mt40">
				<div class="col-gd-740">
					{{> headline}}
					{{> highlight}}
					{{> ads/middle}}
					{{> highlight-box}}
					{{> article}}
					{{> ads/middle}}
					{{> highlight-stories}}
					{{> article}}
					{{> highlight-promo}}
					{{> highlight-photo}}
					{{> article}}
					{{> career}}
					{{> ads/parallax}}
					{{> highlight-video}}
					{{> article}}
					<button class="button buttonMore mt40">More</button>
				</div>
				<div class="col-gd-300">
					<aside>
						{{> ads/right}}
						{{> ads/right mt=20}}
						{{> sidebar/trending}}
						{{> ads/halfpage}}
						{{> sidebar/recomm}}
						{{> sidebar/hashtag}}
						{{> sidebar/highlight}}
						{{> sidebar/writer}}
						{{> sidebar/event}}
						{{> sidebar/highlight-promo}}
					</aside>
				</div>
			</div>
		</section>
	</main>
	{{> ads/sticky}}
	<!-- footer -->
	{{> footer}}

	{{> js}}

</body>

</html>