{{#each catalogMenu}}
{{#gte @index compare=../index_start}}
{{#gte @index compare=../index_end}}
{{else}}
<div class="sortItem">
    <a href="{{link}}" class="sortLink">
        {{title}}
    </a>
</div>
{{/gte}}
{{else}}
{{/gte}}
{{/each}}