{{#each city}}
{{#gte @index compare=../index_start}}
{{#gte @index compare=../index_end}}
{{else}}
<div class="sortItem sortLoc">
    <input type="checkbox" name="loc{{@index}}" class="formCheck sortCheck">
    <span class="sortCheckbox"></span>
    <label class="sortLabel">{{name}}</label>
</div>
{{/gte}}
{{else}}
{{/gte}}
{{/each}}