<!DOCTYPE html>
<html lang="en">
{{> head}}

<body>
	{{> ads/pushdown}}
	<!-- header -->
	{{> header page="Home" login=true}}
	<!-- header -->

	<!-- main -->
	<main role="main" class="main">
		<section class="container">
			{{> event}}
			{{> ads/top}}
			{{> headline}}
			{{> highlight}}
			{{> article}}
			{{> sidebar/trending}}
			{{> ads/middle}}
			{{> sidebar/hashtag}}
			{{> sidebar/highlight}}
			{{> article mt=30}}
			{{> ads/bottom}}
			{{> highlight-stories}}
			{{> article mt=30}}
			{{> ads/middle}}
			{{> sidebar/highlight-promo}}
			{{> highlight-photo}}
			{{> article mt=30}}
			{{> ads/parallax}}
			{{> sidebar/writer}}
			{{> career}}
			{{> highlight-video}}
			{{> article mt=30}}
			<button class="button buttonMore mt30">More</button>
		</section>
	</main>
	{{> ads/sticky}}

	<!-- footer -->
	{{> footer}}

	{{> js}}

</body>

</html>