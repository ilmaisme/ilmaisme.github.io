<!DOCTYPE html>
<html lang="en">
{{> head page=""}}

<body>

    <!-- header -->
    {{> header page="Film, TV, Musik" login=true}}
    <!-- header -->

    <!-- main -->
    <main role="main">
        <div class="container">
            <div class="channelCaption mt30">Film, TV, Musik</div>
        </div>
        {{> ads/skin}}
        {{> ads/billboard}}
        <section class="container">
            <div class="row display-flex mt40">
                <div class="col-gd-740">
                    {{> headline}}
                    {{> highlight}}
                    {{> ads/middle}}
                    {{> article}}
                    {{> ads/middle}}
                    {{> article}}
                    {{> ads/parallax}}
                    {{> article}}
                    <button class="button buttonMore mt40">Selanjutnya</button>
                </div>
                <div class="col-gd-300">
                    <aside>
                        {{> ads/right}}
                        {{> ads/right mt=20}}
                        {{> sidebar/trending}}
                        {{> ads/halfpage}}
                        {{> sidebar/recomm}}
                        {{> sidebar/hashtag}}
                        {{> sidebar/highlight}}
                    </aside>
                </div>
            </div>
        </section>
    </main>
    {{> ads/sticky}}
    <!-- footer -->
    {{> footer}}

    {{> js page="Home" slick=true}}

</body>

</html>